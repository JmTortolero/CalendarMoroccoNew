spring:
  config:
    # Local default for IDE/mvn runs. In Kubernetes, /config/application.yml
    # from Helm (via SPRING_CONFIG_ADDITIONAL_LOCATION) overrides this.
    import: optional:file:./data/calendar/application.yml

